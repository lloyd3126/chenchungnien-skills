interface:
  display_name: "Statement Dog Market"
  short_description: "Read live or saved reports with evidence controls"
  default_prompt: "Use $statementdog-market to analyze this live page or local report archive, record the source mode, build claim and forecast ledgers, apply the relevant domain method, and hand off evidence-backed candidates without treating them as recommendations."
